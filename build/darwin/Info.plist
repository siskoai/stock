<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundlePackageType</key><string>APPL</string>
    <key>CFBundleName</key><string>{{.Info.ProductName}}</string>
    <!-- Doit nommer exactement le binaire présent dans Contents/MacOS, donc
         « outputfilename » de wails.json. Le gabarit {{.Name}} rendrait le nom
         du projet, qui n'est pas le même ici : codesign résout l'exécutable par
         cette clé, et un nom qui ne correspond pas produit une signature
         invalide, que macOS présente comme une application endommagée. -->
    <key>CFBundleExecutable</key><string>Comptoir</string>
    <key>CFBundleIdentifier</key><string>com.comptoir.app</string>
    <key>CFBundleVersion</key><string>{{.Info.ProductVersion}}</string>
    <key>CFBundleShortVersionString</key><string>{{.Info.ProductVersion}}</string>
    <key>CFBundleIconFile</key><string>iconfile</string>
    <key>LSMinimumSystemVersion</key><string>10.15.0</string>
    <key>NSHighResolutionCapable</key><true/>
    <key>NSHumanReadableCopyright</key><string>{{.Info.Copyright}}</string>
    <!-- Application strictement locale : aucune connexion sortante n'est faite. -->
    <key>NSAppTransportSecurity</key>
    <dict><key>NSAllowsArbitraryLoads</key><false/></dict>
</dict>
</plist>
